<h1>Show</h1>

<h3><%= @article.title %></h3>

<%= Kramdown::Document.new(@article.content).to_html.html_safe  %>

<p><%= link_to "Edit", edit_article_path(@article) %> - <%= link_to "Delete", article_path(@article), method: :delete, data: { confirm: "are your sure you want to delete this article?"} %></p>

<%= link_to "All articles", articles_path %>

